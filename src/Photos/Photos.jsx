import React from 'react';
import s from './Photos.module.css';

const Photos = (props) => {
    return (
        <div className={s.photos}>
            <p>Photos</p>
        </div>
    )
};

export default Photos;